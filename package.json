{
  "name": "xingedin",
  "scripts": {
    "dev": "concurrently --kill-others 'npm run dev:gcr-crawler' 'npm run dev:react-frontend'",
    "dev:gcr-crawler": "npm --prefix gcr-crawler run dev",
    "dev:react-frontend": "npm --prefix react-frontend run start"
  },
  "devDependencies": {
    "@types/node": "^14",
    "concurrently": "^7.3.0"
  },
  "dependencies": {
    "@pulumi/docker": "^3.2.0",
    "@pulumi/google-native": "^0.21.0",
    "@pulumi/pulumi": "^3.0.0",
    "@pulumi/random": "^4.8.0"
  }
}
